| Check | Command |
| --- | --- |
| Complete distribution installation suite | `infra packages smoke --root "$INFRA_ROOT" --infra-binary "$INFRA_BINARY" "$PACKAGE_SITE" "$PACKAGE_CHANNELS" full` |
| Quick distribution installation suite | `infra packages smoke --root "$INFRA_ROOT" --infra-binary "$INFRA_BINARY" "$PACKAGE_SITE" "$PACKAGE_CHANNELS" quick` |
