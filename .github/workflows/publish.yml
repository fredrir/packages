name: Publish packages
on:
  workflow_dispatch:
  push:
    branches: [main]
  schedule:
  - cron: '23 4 * * *'
  - cron: '41 5 * * 1'
permissions:
  contents: read
concurrency:
  group: publish
  cancel-in-progress: false
jobs:
  publish:
    permissions:
      contents: write
      id-token: write
    uses: fredrir/infra/.github/workflows/packages-publish.yml@74e98429fb930c484b079be600debd8efc981af5
    secrets: inherit
    with:
      smoke-scope: ${{ github.event.schedule == '41 5 * * 1' && 'full' || 'quick' }}
