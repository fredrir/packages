name: Publish packages
on:
  workflow_dispatch:
  push:
    branches: [main]
  schedule:
  - cron: '23 4 * * *'
  - cron: '41 5 * * 1'
permissions:
  contents: read
concurrency:
  group: publish
  cancel-in-progress: false
jobs:
  publish:
    permissions:
      contents: write
      id-token: write
    uses: fredrir/infra/.github/workflows/packages-publish.yml@76dbc83616a77ce14a8307413286de1b17a914aa
    secrets: inherit
    with:
      smoke-scope: ${{ github.event.schedule == '41 5 * * 1' && 'full' || 'quick' }}
