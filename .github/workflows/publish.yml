name: Publish packages
on:
  workflow_dispatch:
  push:
    branches: [main]
  schedule:
  - cron: '23 4 * * *'
  - cron: '41 5 * * 1'
permissions:
  contents: read
concurrency:
  group: publish
  cancel-in-progress: false
jobs:
  publish:
    permissions:
      contents: write
      id-token: write
    uses: fredrir/infra/.github/workflows/packages-publish.yml@b2c97f0c98c09099d19696089e55f1e95bd6ac1d
    secrets: inherit
    with:
      smoke-scope: ${{ github.event.schedule == '41 5 * * 1' && 'full' || 'quick' }}
